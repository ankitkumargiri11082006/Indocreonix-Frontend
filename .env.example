VITE_CHAT_PROVIDER=openai
VITE_OPENAI_API_KEY=your_openai_api_key_here
VITE_CHAT_MODEL=gpt-4o-mini
VITE_API_BASE_URL=http://localhost:5000/api
# VITE_API_BASE_URL=https://your-backend-domain.com/api

# Session timeout in minutes for both admin and portal users.
# Default fallback in code is 30 if missing/invalid.
VITE_SESSION_TIMEOUT_MINUTES=30

# Optional but recommended: move admin UI off the default /admin route.
# Set this to a hard-to-guess path in production and redeploy.
# Example: VITE_ADMIN_PATH=/admin-9f2a8c0c1b
VITE_ADMIN_PATH=/admin

# Optional Gemini fallback:
# VITE_GEMINI_API_KEY=your_gemini_api_key_here

# Optional OpenRouter fallback:
# VITE_OPENROUTER_API_KEY=your_openrouter_api_key_here

# Note: Google sign-in is handled via backend OAuth redirect.
# Configure these on the BACKEND (not in frontend env):
# - GOOGLE_CLIENT_ID
# - GOOGLE_CLIENT_SECRET
# - FRONTEND_REDIRECT_URI (e.g. https://indocreonix.com)